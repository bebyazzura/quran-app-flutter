import 'package:flutter/material.dart';

class ParaTabs extends StatelessWidget {
  const ParaTabs({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
